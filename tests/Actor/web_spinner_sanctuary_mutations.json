{
  "cwd": "E:/repo/C++RogueLike",
  "test_command": [
    "powershell",
    "-NoProfile",
    "-Command",
    "cmake --build build --config Debug --target test_exe > $null; if ($LASTEXITCODE -ne 0) { exit 1 }; ./build/bin/Debug/test_exe.exe --gtest_filter=WebSpinnerSanctuaryTest.*"
  ],
  "mutations": [
    {
      "file": "src/AiWebSpinner.cpp",
      "original": "\t\tbite(owner, *ctx.player(), ctx);",
      "mutated": "",
      "catches": "a spinner that never bites"
    },
    {
      "file": "src/BuffSystem.cpp",
      "original": "\tconst auto casting = std::ranges::find_if(warded.activeBuffs, is_sanctuary);\n\tif (casting == warded.activeBuffs.end())\n\t{\n\t\treturn false;\n\t}\n\n\t// Already rolled against this casting: the result stands.",
      "mutated": "\tconst auto casting = std::ranges::find_if(warded.activeBuffs, is_sanctuary);\n\tif (casting == warded.activeBuffs.end())\n\t{\n\t\treturn !SavingThrows::is_made(attacker, SavingThrow::SPELL, 0, ctx);\n\t}\n\n\t// Already rolled against this casting: the result stands.",
      "catches": "a save rolled against a player without sanctuary"
    }
  ]
}
